---
import Layout from "../../layouts/Layout.astro";

import { authenticate } from "../../middleware/auth.js";

const response = await authenticate(Astro.request, Astro.redirect);

if (!response.ok) {
  return Astro.redirect("/error");
}
---
<Layout>
    <h2 id="profile">Esperando</h2>
</Layout>
